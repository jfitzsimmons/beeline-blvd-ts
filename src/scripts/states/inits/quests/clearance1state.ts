import { AllQuestsMethods, Quests } from '../../../../types/tasks'
import {
  any_has_value,
  //convos_check,
  does_equal,
  returnfalse,
  // greater_than,
} from '../../../utils/quest'

export const clearance1Quests = (questmethods: AllQuestsMethods): Quests => {
  //const { nq, tq, nvq } = questmethods
  const { nq, nvq } = questmethods

  return {}
}
